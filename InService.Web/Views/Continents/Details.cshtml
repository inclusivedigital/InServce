@model Continent

@section subnav{
    <div class="navbar-nav">
        <div class="nav-item"><a class="nav-link navbar-brand" href="@Url.Index()"><i class="mdi mdi-chevron-double-left"></i> Continents</a></div>
        <div class="nav-item"> <a class="nav-link" href="@Url.Details()">@Model.Name</a></div>
        <div class="nav-item"><a class="nav-link" href="@Url.Edit()"><i class="mdi mdi-pencil"></i> Edit...</a></div>
    </div>
}


<h3 class="font-weight-light"><i class="mdi mdi-map"></i>Continent : @Model.Name</h3>
<div class="row">
    <div class="col-md-6">
        <div class="card shadow">
            <div class="card-body">
                <div class="form-group">
                    <label class="font-weight-bold text-muted mb-0">Name</label>
                    <div>@Model.Name</div>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        @if (Model.Regions.Any())
        {
            <div class="form-inline">
                <h4 class="font-weight-light">Regions</h4>
                <div class="ml-auto">
                    <a href="@Url.Add("Regions",Model.ID)"><i class="mdi mdi-plus"></i> Add more</a>
                </div>
            </div>
            <table class="table table-sm table-striped table-hover">
                <thead>
                    <tr>
                        <th>Name</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Regions)
                    {
                        <tr>
                            <td><a href="@Url.Details("Regions",new { item.ID})">@item.Name</a></td>
                        </tr>
                    }
                </tbody>
            </table>
        }
        else
        {
            <div class="text-center">
                <h4 class="font-weight-light text-danger">No region in this continent</h4>
                <a class="btn btn-outline-success btn-sm" href="@Url.Add("Regions",Model.ID)"><i class="mdi mdi-plus"></i> Add more</a>
            </div>
        }
    </div>

</div>