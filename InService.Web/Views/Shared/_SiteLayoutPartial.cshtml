<header class="header_area">
    <div class="header-top">
        <div class="container">
            <div class="row align-items-center">
                <div class="col-lg-6 col-sm-6 col-4 header-top-left">
                    <a href="tel:+11">
                        <span class="lnr lnr-phone"></span>
                        <span class="text">
                            <span class="text">+263</span>
                        </span>
                    </a>
                    <a href="mailto:support@inclusivedital.tech">
                        <span class="lnr lnr-envelope"></span>
                        <span class="text">
                            <span class="text">support@inclusivedital.tech</span>
                        </span>
                    </a>
                </div>
                <div class="col-lg-3 col-sm-3 col-6 header-top-right">
                    <a href="#" class="nav-link search" id="search">
                        <i class="lnr lnr-magnifier"></i>
                    </a>
                </div>
                <div class="col-lg-3 col-sm-3 col-6 header-top-right">
                    <a href="@Url.Action("Login","Account")" class="text-uppercase">Login</a>
                </div>

            </div>
        </div>
    </div>

    <div class="main_menu">
        <div class="search_input" id="search_input_box">
            <div class="container">
                <form class="d-flex justify-content-between" method="" action="">
                    <input type="text" class="form-control" id="search_input" placeholder="Search Here">
                    <button type="submit" class="btn"></button>
                    <span class="lnr lnr-cross" id="close_search" title="Close Search"></span>
                </form>
            </div>
        </div>

        <nav class="navbar navbar-expand-lg navbar-light">
            <div class="container">

                <!-- Brand and toggle get grouped for better mobile display -->
                <a class="navbar-brand logo_h" href="#">
                    <img src="~/Content/COA.png" class="img-fluid" style="width:70px;" />
                </a>
                <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
                        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                </button>
                <!-- Collect the nav links, forms, and other content for toggling -->
                <div class="collapse navbar-collapse offset" id="navbarSupportedContent">
                    <ul class="nav navbar-nav menu_nav ml-auto">
                        <li class="nav-item active navbar-menu "><a class="nav-link" href="@Url.Action("Article","Home",new {DefaultID=1 })">Home</a></li>
                        @*  @if (SiteManager.Courses.Any())
                            {
                                foreach (var course in SiteManager.Courses)
                                {
                                    <li class="nav-item submenu dropdown">
                                        <a href="@Url.Action("Course","Home",new { CourseID=course.ID})" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                                           aria-expanded="false">@course.Name</a>
                                        <ul class="dropdown-menu">
                                            @if (course.Modules.Any())
                                            {
                                                foreach (var module in course.Modules.OrderBy(c => c.Number))
                                                {
                                                    <li class="nav-item"><a class="nav-link" style="font-weight:lighter;font-size:10px;text-transform:none;" href="@Url.Action("Article","Home",new { ModuleID=module.ID})">@module.Name</a></li>
                                                }
                                            }
                                        </ul>
                                    </li>
                                }
                            }

                        *@


                        @if (SiteManager.Branches.Any())
                        {
                            foreach (var branch in SiteManager.Branches.Where(c => c.Courses.Any()))
                            {
                                <li class="nav-item submenu dropdown">
                                    <a style="font-weight:lighter;font-size:12px;text-transform:none;" href="@Url.Action("Branch","Home",new { BranchID=branch.ID})" class="nav-link dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true"
                                       aria-expanded="false">@branch.Name</a>
                                    <ul class="dropdown-menu">
                                        @if (branch.Courses.Any())
                                        {
                                            foreach (var course in branch.Courses.OrderBy(c => c.Name))
                                            {
                                                <li class="nav-item"><a class="nav-link" style="font-weight:lighter;font-size:10px;text-transform:none;" href="@Url.Action("Course","Home",new { CourseID=course.ID})">@course.Name</a></li>
                                            }
                                        }
                                    </ul>
                                </li>
                            }
                        }
                        @*<li class="nav-item">
                                <a href="#" class="nav-link search" id="search">
                                    <i class="lnr lnr-magnifier"></i>
                                </a>
                            </li>*@
                    </ul>
                </div>
            </div>
        </nav>
    </div>
</header>




















